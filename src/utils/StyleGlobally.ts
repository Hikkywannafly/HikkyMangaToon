import { StyleSheet } from 'react-native';
import { useFontDefault } from '@/hooks';



const StyleGlobally = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        fontFamily: 'Dongle-Regular',

    },
});
